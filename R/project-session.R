weight_kilo <- 10
# Comment:Use Alt-Minus to make <-
weight_kilo


# Vectors:Character
c("a", "b")
# Numeric
c(1, 2)
# Logic (TRUE, False)

# data frame
head(iris)

colnames(iris)
str(iris)
summary(iris)



# #Exercise ---------------------------------------------------------------

# Object names
#day one
Day_One

#day one
day_one

# T <- FALSE
false <- FALSE


#c <- 9
number_value <- 9

sum_vector <- function(x) sum(x)

# Spacing
x[, 1]
x[, 1]
x[, 1]
mean (x, na.rm = TRUE)
mean( x, na.rm = TRUE )
function (x) {}
function(x) {}
height<-feet * 12 + inches
mean(x, na.rm = 10)
sqrt(x^ 2 + y ^ 2)
df$z
x <- 1 : 10

# Indenting
if (y < 0 && debug)
  message("Y is negative")


x[,1]

weight <- -10 * 10 + 2
